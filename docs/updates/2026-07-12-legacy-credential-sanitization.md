# 旧 MVC 配置凭据清理（2026-07-12）

## 改动

- `szaipa2022/Web.config` 的 Szaipa/Tongou 数据库主机、用户名、密码改为部署占位符。
- 同步清理跟踪中的生成副本 `szaipa2022/bin/szaipa2022.dll.config`。
- 现代项目仍使用被 `.gitignore` 排除的 `src/Szaipa.Web/appsettings.Local.json` 注入本地只读连接；已跟踪的现代配置只有空值或示例占位符。

## 必须人工完成

旧真实凭据曾存在于 Git 历史和旧发布副本中。修改当前分支不能使旧密码失效，也不能清除已有克隆/部署文件；必须在数据库服务器轮换相关账号密码。轮换前应视为已经泄露。

本次没有改写 Git 历史，避免破坏已共享分支。若未来决定彻底清除历史对象，需要单独安排全仓历史重写和所有协作者重新克隆。
